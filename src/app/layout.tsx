import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: {
    default: "Quest Interactive | AI Automation for SMEs",
    template: "%s | Quest Interactive",
  },
  description:
    "Quest Interactive helps SMEs convert website and WhatsApp inquiries into qualified booked demos automatically with 24/7 AI response workflows.",
  keywords: [
    "AI automation agency",
    "SME lead automation",
    "WhatsApp AI assistant",
    "automated booking",
    "lead qualification",
  ],
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html
      lang="en"
      className={`${geistSans.variable} ${geistMono.variable} h-full antialiased`}
    >
      <body className="min-h-full flex flex-col">{children}</body>
    </html>
  );
}
